import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Neon Brutalist Playground",
  description:
    "A radical neobrutalist microsite with kinetic layouts, scroll-triggered motion, and bold chromatic energy.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-grid selection:bg-[var(--accent)] selection:text-[var(--foreground)]`}
      >
        {children}
      </body>
    </html>
  );
}
